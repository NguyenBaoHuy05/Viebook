name: Custom Webhook

on:
  push:
    branches:
      - main

jobs:
  send_webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Kiểm tra URL Webhook
        run: echo "Webhook URL: ${{ secrets.DISCORD_WEBHOOK_URL }}"

      - name: Gửi JSON đến Webhook
        run: |
          curl -X POST "${{ secrets.DISCORD_WEBHOOK_URL }}" \
               -H "Content-Type: application/json" \
               -d '{
                 "content": "🚀 **New Push in Viebook repo!**",
                 "username": "GitHub Notifier",
                 "embeds": [{
                   "title": "📌 Repository Update",
                   "description": "**👤 Pushed by:** '${{ github.actor }}'\n**📝 Commit Message:** '${{ github.event.head_commit.message }}'",
                   "url": "https://github.com/${{ github.repository }}/commits",
                   "color": 16776960
                 }]
               }'
